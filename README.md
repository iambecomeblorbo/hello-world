# **FISH!!!!** 🐟😍

hello from Blorbo.


## 🧊 Working With Blorbo’s Repo (SSH)

To use Blorbo’s SSH identity, clone with the Blorbo host alias:

```bash
git clone git@github.com-blorbo:iambecomeblorbo/hello-world.git
```

Then set the local Git identity for all commits in this repo:

```bash
git config user.name "iambecomeblorbo"
git config user.email "YOUR_BLORBO_EMAIL"
```

Confirm:

```bash
git config user.name
git config user.email
```
